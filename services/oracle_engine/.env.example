# Oracle Engine environment variables
KAFKA_BROKERS=localhost:9092
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Oracle TLS certificate pins (SHA-256 fingerprints)
# IMD_CERT_FINGERPRINT=
# ACCUWEATHER_CERT_FINGERPRINT=
# ... and so on
